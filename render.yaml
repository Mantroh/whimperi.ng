services:
  - type: web
    name: whimpering-chat
    env: node
    buildCommand: cd frontend && npm install && npm run build && cd ../backend && npm install
    startCommand: cd backend && node server.production.js
    envVars:
      - key: NODE_ENV
        value: production
